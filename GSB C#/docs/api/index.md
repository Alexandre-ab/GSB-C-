# Référence API

Cette section est générée automatiquement par DocFX à partir du projet `GSB C#.csproj`. Après exécution de `docfx metadata` puis `docfx build`, les classes, méthodes et propriétés seront accessibles depuis le menu de gauche comme dans l’exemple gsbMonolith DAO.

